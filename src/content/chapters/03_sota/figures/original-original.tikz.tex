\documentclass[tikz]{standalone}

\usepackage[margin=1in]{geometry}
\usepackage[dvipsnames]{xcolor}
\usetikzlibrary{shapes, shapes.multipart, arrows, positioning}

\begin{document}
  % LTeX: enabled=false

  \tikzstyle{source} = [
    draw, rectangle, rounded corners, 
    inner sep=6pt, align=center,
    draw=yellow, fill=yellow!30, line width=1pt,
    minimum height=1cm,
  ]
  
  \tikzstyle{process} = [
    draw, rectangle split, rectangle split horizontal, rectangle split parts=3, 
    inner sep=6pt, align=center, 
    fill=gray!10, draw=gray, line width=1pt,
    minimum height=1cm,
  ]

  \begin{tikzpicture}[node distance = 1cm, auto, font=\sffamily]
    
    \node [source] (initial) {Initial search};
    \node [process, below right = of initial] (duplicate) {\nodepart{two} Duplicate removal};
    \node [source, below left= of duplicate] (structured) {Structured search};


    \node [
      draw, rectangle split, rectangle split, rectangle split parts=6,
      rectangle split part fill={Purple!30, Purple!30, Purple!30, Cyan!30, Cyan!30, Cyan!30},
      inner sep=6pt, align=left,
      below = of structured
    ] (sources) {
      \nodepart{one} Google Scholar
      \nodepart{two} Microsoft Academic
      \nodepart{three} Semantic Scholar
      \nodepart{four} ScienceDirect
      \nodepart{five} IEEE Xplore
      \nodepart{six} ACM Digital Library
    };

  \end{tikzpicture}
\end{document}